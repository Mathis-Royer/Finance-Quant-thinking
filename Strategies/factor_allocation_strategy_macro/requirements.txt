# Factor Allocation Strategy - Requirements
# ==========================================

# Core Data Science
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
pyarrow>=14.0.0

# Machine Learning - Metrics (for AUC calculation)
scikit-learn>=1.3.0

# Machine Learning - Deep Learning (Transformer model)
torch>=2.0.0

# Data Sources (for production use)
# ---------------------------------
# Uncomment as needed based on your data source subscriptions

# Free APIs
# fredapi>=0.5.0           # FRED/ALFRED (US macro data) - for individual series
pandas-datareader>=0.10    # Kenneth French factor data
# yfinance>=0.2.0          # Yahoo Finance (factor ETF proxies)

# FRED-MD Data (included - downloads directly from St. Louis Fed)
# No additional packages required - uses pandas to read CSV from:
# https://files.stlouisfed.org/files/htdocs/fred-md/monthly/current.csv
requests>=2.28.0           # For downloading FRED-MD data

# Paid APIs (require subscriptions)
# bloomberg-api            # Bloomberg Terminal ($24k/year)
# refinitiv-data           # Refinitiv Eikon ($15-22k/year)
# tradingeconomics>=4.0    # Trading Economics ($49-299/month)
# nasdaq-data-link>=1.0    # Quandl/Nasdaq Data Link (freemium)

# Hyperparameter Tuning (optional - falls back to random search if not installed)
optuna>=3.0.0

# Development & Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Visualization (optional)
matplotlib>=3.7.0
plotly>=5.15.0

# Utilities
tqdm>=4.65.0
python-dateutil>=2.8.0
